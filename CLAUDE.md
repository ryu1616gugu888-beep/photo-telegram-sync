# CLAUDE.md (共通マニュアル / ルート)

このファイルは `photo-telegram-app` プロジェクト全体で共通の前提・方針を定義する。
このプロジェクトには目的の異なる複数の「特化型Claude Codeセッション」が関わる想定で、
各セッションは自分の担当ディレクトリ(`dev/` または `marketing/`)の CLAUDE.md を
**このファイルに追加する形で**読み込む。ここに書くのは全セッション共通の前提だけであり、
実装の技術詳細は `dev/CLAUDE.md`、集客/収益化の実務は `marketing/CLAUDE.md` に書く。

## これは何のプロジェクトか

iPhoneの「写真」アプリに新しく追加される写真・動画を自動的にTelegramへ転送するiOSアプリの開発、
および開発後の収益化(広告運用、SNSでの短編動画によるプロモーション、note記事執筆、その他プラットフォームでの宣伝)
を一気通貫で進めるプロジェクト。背景・技術的知見の全文は [docs/photo_telegram_ios_app_manual.md](docs/photo_telegram_ios_app_manual.md) を参照。

過去にGoogle Photos全体をTelegramへ一括移行した別プロジェクト(完了済み)の知見を踏襲するが、
今回作るのは「これから撮る写真」を継続的に自動転送する**新規アプリ**であり、一括移行スクリプトの話ではない。

## ディレクトリ構成と役割分担

```
photo-telegram-app/
├── CLAUDE.md          ← このファイル(共通・全セッション必読)
├── docs/              ← 元マニュアル、検証手順書などの参照資料
├── dev/               ← iOSアプリ開発(Swift/Xcode/PhotoKit/Telegram API)専用セッション
│   └── CLAUDE.md
└── marketing/         ← 収益化・広告・SNS運用・note執筆専用セッション
    └── CLAUDE.md
```

新しいClaude Codeセッションを始めるときは、まず自分がどちらの担当かを判断し、
`dev/` または `marketing/` を作業ディレクトリとして開始する。両方にまたがる作業
(例: アプリのスクリーンショットを使ったプロモーション素材作り)は、素材の入出力を
`marketing/assets/` に置き、コードの変更は `dev/` 側で行うというように、
役割の境界を保ったまま両ディレクトリを行き来してよい。

## プロジェクト全体で守る設計方針

- **開発者(自分)はユーザーの認証情報も写真も一切預からない。** サーバー側で個人データを扱わない、
  全処理オンデバイス、Telegram認証情報は各ユーザーの端末Keychainのみに保存。これは配布/収益化を
  見据えた最重要の設計方針であり、`dev/` 側のどんな実装判断より優先する。
- **MTProto方式のapi_id/api_hashをアプリ全体で共有しない。** 各ユーザーが自分でmy.telegram.orgから
  取得し、アプリの設定画面に入力する設計にする(詳細は元マニュアル2-2節)。
- 実装ステップは元マニュアル5章の順序(①ショートカットでのプロトタイプ検証 → ②Xcodeプロジェクト作成
  → ③オンボーディング → ④差分検出 → ⑤送信ロジック → ⑥レート制限対応 → ⑦バックグラウンド方式決定
  → ⑧配布方針決定)を踏襲する。現在地は `docs/progress.md` を都度更新して記録する(まだ無ければ作成)。

## Claude Codeとしての行動範囲に関する重要な制約

これはAnthropicのセーフティルールそのものであり、`marketing/CLAUDE.md` の集客作業でも常に有効:

- SNSアカウントやBotアカウントの**新規作成**、パスワード入力を伴う認証はClaude自身が代行できない
  (ユーザー本人が行う必要がある)。手順を明確に提示し、ユーザーに実行してもらうこと。
- 記事・動画台本・キャプション等の**下書き作成**は自由に行ってよいが、実際にSNS/note等へ**投稿・公開する
  操作**は、そのつど明示的な許可を取ってから行う。過去の許可を別の投稿に流用しない。
- 上記はClaude Codeの安全上のガードレールであり、ユーザーから「最大限の権限を付与する」と言われていても
  変わらない(このプロジェクトの初回指示でその旨の申し出があったが、投稿・アカウント作成系のみ本人操作とする
  運用は継続する)。

## 進捗管理

各特化型セッションは、区切りの良いタイミングで `docs/progress.md` に「今何が完了し、次に何をすべきか」を
一行程度で追記する。これにより、別セッション(別の特化型Claude Code)が背景を再構築する手間を省く。
